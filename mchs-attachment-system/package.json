{
  "name": "mchs-attachment-system",
  "version": "1.0.0",
  "private": true,
  "description": "Clinical Attachment Allocation System — Malawi College of Health Sciences, Zomba Campus. Single-service deploy: Express serves the built React client.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "npm start --prefix server"
  }
}
